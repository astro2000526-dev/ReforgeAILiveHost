# Reforge AI Live Host — Full Story (English)

_Generated by /kasi-graph · grep parser caught 50 TS/JS fns; Python pipeline narrated from source read._

## Cast (where the code lives)

| Tier | Folder | Runtime | Role |
|---|---|---|---|
| **Frontend** | `apps/web/src/app/projects/new/page.tsx` | Browser (React) | seller fills wizard |
| **Frontend** | `apps/web/src/app/projects/[id]/ProjectActionPanel.tsx` | Browser (React) | seller starts generation + RTMP |
| **API gateway** | `apps/web/src/app/api/**/route.ts` | Next.js server (Vercel) | auth + DB write + forward to pipeline |
| **Pipeline** | `services/pipeline/app/main.py` | FastAPI (UCloud GPU) | accept jobs, do work |
| **Pipeline core** | `services/pipeline/app/pipeline.py` | FastAPI | TTS → concat → MuseTalk → GFPGAN → transcode |
| **Streaming** | `services/pipeline/app/streaming.py` | FastAPI subprocess | ffmpeg RTMP loop |

## Full call chain — 1 seller, 1 livestream, end-to-end

```
[1] BROWSER — wizard
    NewProjectPage()                              new/page.tsx:58
      ├─ fetch('/api/avatars')                    (load templates)
      ├─ detectLanguage(text)                     new/page.tsx:47  ← regex Thai range
      ├─ generateScript()                         new/page.tsx:105
      │     POST /api/projects/draft/script
      │     → returns 6 segments (intro/pain/product/demo/price/cta)
      └─ saveProject()                            new/page.tsx:141
            POST /api/projects
            → router.push(`/projects/${id}`)

[2] NEXT.JS API — apps/web/src/app/api/
    POST /api/projects                            api/projects/route.ts
      ├─ supabaseAdmin.from('projects').insert({...})
      └─ return { project: { id } }

[3] BROWSER — project detail page
    ProjectActionPanel()                          [id]/ProjectActionPanel.tsx:36
      └─ startGeneration()                        ProjectActionPanel.tsx:87
            POST /api/projects/:id/generate

[4] NEXT.JS API — generate proxy
    POST /api/projects/[id]/generate              [id]/generate/route.ts
      ├─ supabaseAdmin.select projects + joined avatar
      ├─ randomUUID() → generation_id
      ├─ supabaseAdmin.insert generations (status=queued)
      ├─ supabaseAdmin.update projects.status='generating'
      └─ pipelineFetch('/generate', body={
            project_id, generation_id,
            avatar_template_url, script_segments, voice, rate })
            ↓ HTTP across the internet ↓

[5] FASTAPI — receive job
    @app.post('/generate')                        pipeline/app/main.py:69
      ├─ _jobs[project_id] = {status:'queued'}    (in-memory dict)
      └─ background.add_task(_run_job, req)       (FastAPI BackgroundTasks)
      ← returns 202 immediately

[6] FASTAPI — background worker
    _run_job(req)                                 main.py:35
      └─ generate(GenerateInput(...), progress)   pipeline.py:63
            ├─ progress('download', 5)
            │     _download(avatar_template_url) → template.mp4
            ├─ progress('tts', 20)
            │     synthesize_all(segments, voice, rate)   tts/__init__.py:82
            │       ├─ _provider_for_voice(voice)          ← Azure if 'th-TH-...', Volcengine if 'BV...'
            │       └─ asyncio.gather: provider.synthesize each segment in parallel
            │             → seg_00.mp3, seg_01.mp3, ...
            ├─ progress('concat_audio', 40)
            │     concat_audio(seg_audios, full.mp3)        ffmpeg_ops.py
            ├─ progress('lipsync', 60)
            │     run_lipsync(template.mp4, full.mp3, lipsynced.mp4)   musetalk.py
            │       (MuseTalk CLI subprocess on GPU)
            ├─ progress('face_restore', 75)
            │     restore_faces(lipsynced.mp4, restored.mp4)   face_restore.py
            │       (GFPGAN, mouth-edge cleanup)
            ├─ progress('transcode', 85)
            │     transcode_for_streaming(restored.mp4, final.mp4)   ffmpeg_ops.py
            │       (libx264+aac, GOP=2s, ready for RTMP)
            ├─ progress('upload', 95)
            │     upload_video(final.mp4, project_id, gen_id)   storage.py
            │       → Supabase Storage public URL
            └─ progress('done', 100)
                  _jobs[pid] = {status:'done', output_url:...}

[7] BROWSER — polling loop
    tick()                                        ProjectActionPanel.tsx:61
      every 3s: fetch('/api/projects/:id/generation-status')

[8] NEXT.JS API — status proxy + writeback
    GET /api/projects/[id]/generation-status      generation-status/route.ts
      ├─ pipelineFetch(`/generate/${id}`)            ← reads _jobs[id]
      ├─ if terminal:
      │     supabaseAdmin.update generations.status='done'/'failed'
      │     supabaseAdmin.update projects.status='ready'/'failed'
      └─ return body
    → browser sees status='done' → flips UI to 'ready'

[9] BROWSER — start live
    startStream()                                 ProjectActionPanel.tsx:103
      POST /api/streams/start { project_id, rtmp_url, stream_key }

[10] NEXT.JS API — stream proxy
    POST /api/streams/start                       streams/start/route.ts
      ├─ supabaseAdmin.select projects (check ready)
      ├─ supabaseAdmin.select generations latest done → video_url
      ├─ supabaseAdmin.insert streams (status=live, stream_key)
      └─ pipelineFetch('/stream/start', {
            stream_id, video_url, rtmp_url, stream_key })

[11] FASTAPI — start RTMP push
    @app.post('/stream/start')                    main.py:99
      └─ start_stream(stream_id, video_input, rtmp_url, stream_key)   streaming.py:26
            └─ subprocess.Popen([
                  ffmpeg -re -stream_loop -1 -i video
                  -c:v libx264 -preset veryfast -b:v 3000k
                  -force_key_frames 'expr:gte(t,n_forced*2)'
                  -c:a aac -b:a 128k -ar 44100
                  -f flv  rtmp_url/stream_key
               ])
            _streams[stream_id] = handle
      → ffmpeg pushes frames out to Shopee/Douyin/Xiaohongshu/...

[12] LIVE — viewers watch on Shopee
    Shopee CDN ingests RTMP → fans out to viewers worldwide
    seller can close laptop; server keeps streaming 24/7

[13] BROWSER — stop
    stopStream()                                  ProjectActionPanel.tsx:126
      POST /api/streams/:id/stop
    → FastAPI stop_stream() terminates ffmpeg subprocess
    → supabaseAdmin streams.status='stopped'
```

## Files in the subgraph

### Frontend (Browser)
- `apps/web/src/app/projects/new/page.tsx` — 4-step wizard (avatar → product → script → voice)
- `apps/web/src/app/projects/[id]/page.tsx` — project detail SSR
- `apps/web/src/app/projects/[id]/ProjectActionPanel.tsx` — start/poll/stop client component
- `apps/web/src/app/dashboard/page.tsx` — project list

### API gateway (Next.js routes)
- `apps/web/src/app/api/projects/route.ts` — POST create project
- `apps/web/src/app/api/projects/[id]/route.ts` — GET project
- `apps/web/src/app/api/projects/[id]/generate/route.ts` — start generation, forward to FastAPI
- `apps/web/src/app/api/projects/[id]/generation-status/route.ts` — poll, mirror status to DB
- `apps/web/src/app/api/projects/draft/script/route.ts` — auto-generate 6-segment script
- `apps/web/src/app/api/projects/[id]/script/route.ts` — edit script
- `apps/web/src/app/api/avatars/route.ts` — list avatar templates
- `apps/web/src/app/api/streams/start/route.ts` — start RTMP via FastAPI
- `apps/web/src/app/api/streams/[id]/stop/route.ts` — stop RTMP

### API helpers
- `apps/web/src/lib/pipeline-client.ts` — `pipelineFetch()` — typed forwarder, handles unconfigured/HTTP errors
- `apps/web/src/lib/supabase-server.ts` — `supabaseAdmin` service-role client
- `apps/web/src/lib/demo-user.ts` — hardcoded `DEMO_USER_ID` (auth not wired yet)

### Pipeline (FastAPI)
- `services/pipeline/app/main.py` — HTTP endpoints + in-memory job dict
- `services/pipeline/app/pipeline.py` — `generate()` orchestrator, the heart
- `services/pipeline/app/tts/__init__.py` — `synthesize_all()` + `_provider_for_voice()` router
- `services/pipeline/app/tts/azure.py` — Azure Neural TTS (Thai voices)
- `services/pipeline/app/tts/volcengine.py` — Volcengine TTS (Chinese voices)
- `services/pipeline/app/tts/edge.py` — edge-tts fallback
- `services/pipeline/app/ffmpeg_ops.py` — `concat_audio()`, `transcode_for_streaming()`
- `services/pipeline/app/musetalk.py` — `run_lipsync()` MuseTalk CLI wrapper
- `services/pipeline/app/face_restore.py` — `restore_faces()` GFPGAN
- `services/pipeline/app/storage.py` — `upload_video()` to Supabase Storage
- `services/pipeline/app/streaming.py` — `start_stream()` / `stop_stream()` ffmpeg subprocess
- `services/pipeline/app/schemas.py` — Pydantic request/response models
- `services/pipeline/app/config.py` — env config, paths

### Data
- `supabase/migrations/0001_initial_schema.sql` — projects, generations, streams, avatars
- `supabase/migrations/0002_demo_seed.sql` — demo data

## Hotspots (TS/JS only — Python not parsed)

| Function | Callers | File |
|---|---|---|
| `cn` (className util) | 11 | `lib/utils.ts:3` |
| `ProjectActionPanel` | 8 | `[id]/ProjectActionPanel.tsx:36` |
| `startGeneration` | 8 | (same) |
| `startStream` | 8 | (same) |
| `stopStream` | 8 | (same) |
| `tick` (status poll) | 8 | (same) |
| `detectLanguage` | 7 | `new/page.tsx:47` |
| `generateScript` | 7 | (same) |
| `saveProject` | 7 | (same) |

→ Hot zone = `ProjectActionPanel` (5 of top 10) and `NewProjectPage` (4 of top 10). Most regressions will be here.

## Cycles
None detected.

## Caveats

- Python pipeline not in graph (grep parser only handles PHP/JS/TS). I read the source by hand for sections 5–6, 10–11 above.
- `_streams` and `_jobs` are module-level dicts in FastAPI — state lost on container restart. Will need to push state to Supabase rows for production durability.
- `DEMO_USER_ID` is hardcoded — auth boundary not yet enforced.
