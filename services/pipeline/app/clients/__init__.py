"""External network API clients (no GPU, no orchestration).

TTS provider clients (Azure / Volcengine / edge) live in app/tts/ — they
predate this package and already follow the same one-client-per-module rule.
"""
